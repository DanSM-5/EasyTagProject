@model Appointment

@{
    ViewData["Title"] = "Confirmation";
}

<h1>Appointment made successful</h1>

<div>
    <p>Your appointment on @Model.Start.ToShortDateString() @Model.Start.ToShortTimeString() for @Model.Description was made successful.</p>
    <p>Do you want to repeat the appointment more days? <a class="btn btn-primary"
                                                           asp-action="RepeatAppointment" 
                                                           asp-controller="AppointmentCRUD" 
                                                           asp-route-code="@Model.RoomCode"
                                                           asp-route-id="@Model.Id"
                                                           asp-route-start="@Model.Start.ToString("MM-dd-yyyy H:mm")"
                                                           asp-route-end="@Model.End.ToString("MM-dd-yyyy H:mm")">Select options</a></p>
    <p><a asp-action="Room" asp-controller="Room"
          asp-route-code="@Model.RoomCode"
          asp-route-pdate="@Model.Start.ToString("MM-dd-yyyy")"><i class="fas fa-arrow-alt-circle-left fa-2x"></i> Go back to room @Model.RoomCode page.</a></p>
</div>
